import { KilometroParaCentimetroPipe } from './kilometro-para-centimetro.pipe';

describe('KilometroParaCentimetroPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometroParaCentimetroPipe();
    expect(pipe).toBeTruthy();
  });
});
